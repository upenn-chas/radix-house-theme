{#
/**
 * @file
 * Template for Navbar component.
 *
 * Available config:
 * - container: fixed | fluid.
 * - placement: default | fixed-top | fixed-bottom | sticky-top
 * - color: light | dark
 * - navbar_expand: sm | md | lg | xl
 * - utility_classes: An array of utility classes.
 *
 * Available blocks:
 * - branding
 * - left
 * - right
 */
#}
{{ attach_library('radix/navbar') }}

{% set container = container == 'fluid' ? 'container' : false %}
{% set placement = placement ?? 'fixed-bottom' %}
{% set color = color ?? 'dark' %}
{% set navbar_expand = navbar_expand ?? 'lg' %}

<nav class="navbar navbar-expand-{{ navbar_expand }} justify-content-between navbar-{{ color }} {{ utility_classes|join(' ') }}">
  {% if container %}
    <div class="{{ container }}">
  {% endif %}
  {% block branding %}
<div id="logo" class="home-link"> 
   {# {{ branding }} #} 
  {% endblock %}
</div>
  <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbar-collapse" aria-expanded="false" aria-label="Toggle navigation">
  <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse">
    {% block left %}
      {{ left }}
    {% endblock %}

    {% block right %}
      {{ right }}
    {% endblock %}
  </div>

  {% if container %}
    </div>
  {% endif %}
</nav>
